# Баг: Отзывы не работают после завершения поездки

## Проблема

После завершения поездки пользователь не может оставить отзыв. Приложение показывает:
```
⚠️ Rental status is "current", not "finished"
```

---

## Анализ логов Flutter

### 1. Завершение поездки — УСПЕШНО

```
🏁 Finishing trip 159
🔑 Vehicle API Token: present
🌐 Vehicle API Request: PATCH https://rentalguru.ru/chat/trips/159/
✅ Vehicle API Response: 200
✅ Trip 159 finished: {id: 159, ..., status: finished, set_rating: true, set_feedback: true, ...}
```

**Вывод:** Сервер вернул `status: finished` — поездка завершена корректно.

### 2. Загрузка списка rentals

```
📋 Loaded 24 rentals
🔍 Loading vehicle: /vehicle/autos/142/
...
```

### 3. Попытка оставить отзыв — ОШИБКА

```
⭐ Submitting rating for rental: id=159, status=current
⭐ Full rental data: {id: 159, ..., status: current, ...}
⚠️ Rental status is "current", not "finished"
```

**Проблема:** При отправке рейтинга Flutter использует `status: current`, хотя сервер вернул `status: finished`.

---

## Проверка бэкенда

### База данных

```sql
SELECT id, status FROM chat_trip WHERE id = 159;
```

| id  | status   |
|-----|----------|
| 159 | finished |

**Вывод:** В БД статус корректный — `finished`.

### API ответ

PATCH `/chat/trips/159/` возвращает:
```json
{
  "id": 159,
  "status": "finished",
  "set_rating": true,
  "set_feedback": true,
  ...
}
```

**Вывод:** Бэкенд работает корректно.

---

## Причина бага

Flutter после успешного PATCH **не обновляет локальный объект rental** в памяти.

Последовательность:
1. Пользователь нажимает "Завершить поездку"
2. Flutter отправляет PATCH, получает ответ с `status: finished`
3. Flutter **не обновляет** локальный объект rental
4. При попытке оставить отзыв Flutter проверяет статус из **старого объекта** (`current`)
5. Проверка не проходит → отзыв не отправляется

---

## Рекомендации для фронтенда

### Вариант 1: Обновить локальный объект после PATCH (рекомендуется)

```dart
// После успешного PATCH
final response = await api.patch('/chat/trips/$tripId/', {'status': 'finished'});
if (response.statusCode == 200) {
  // Обновить локальный объект данными из ответа
  rental.status = response.data['status'];  // 'finished'
  rental.setRating = response.data['set_rating'];
  rental.setFeedback = response.data['set_feedback'];
}
```

### Вариант 2: Перезагрузить rental перед отправкой рейтинга

```dart
// Перед отправкой рейтинга
final freshRental = await api.get('/chat/trips/$tripId/');
if (freshRental.status == 'finished') {
  await submitRating(freshRental);
}
```

### Вариант 3: Убрать клиентскую проверку статуса

Доверять серверу — если статус неверный, сервер сам вернёт ошибку.

```dart
// Убрать эту проверку:
// if (rental.status != 'finished') { return; }

// Просто отправлять рейтинг, сервер проверит сам
await submitRating(rental);
```

---

## Резюме

| Компонент | Статус |
|-----------|--------|
| Бэкенд API | ✅ Работает корректно |
| База данных | ✅ Данные корректны |
| Flutter | ❌ Не обновляет локальный объект после PATCH |

**Исправление требуется на стороне Flutter.**
